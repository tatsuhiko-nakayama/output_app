<%= @item.title %>
<%= @item.tagbody %>
<%= @item.url %>
<%= @item.body %>
<%= @item.category.name%>
<%= link_to '編集', edit_item_path(@item.id) %>
<%= link_to '削除', "/items/#{@item.id}", method: :delete %>